import 'face_recognition_page.dart';

/// Separate route kept to match the mobile implementation guide.
/// It currently reuses the same page because enrolment is included there.
class FaceEnrolmentPage extends FaceRecognitionPage {
  const FaceEnrolmentPage({super.key});
}
